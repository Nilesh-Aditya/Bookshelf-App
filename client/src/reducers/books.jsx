const initialState = {
    status = 'idle',
    entries:{}
};


export default function bookReducer(state = initialState, action){
    const { payload: data } = action;  
    
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}